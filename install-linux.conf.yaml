- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc: shell/zshrc
    ~/.gitconfig: gitconfig
    ~/.aliases: shell/aliases

- shell:
    - command: setup/setup.sh
      stdout: true
      stderr: true
