- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc: shell/zshrc
    ~/.gitconfig: gitconfig
    ~/.aliases: shell/aliases

- shell:
    - command: setup/mac/brew.sh
      stdout: true
      stderr: true
    - command: setup/zsh.sh
      stdout: true
      stderr: true
